{
  "name": "sync_third_party_app_data",
  "description": "Synchronizes user data from a specified third-party app.",
  "strict": false,
  "parameters": {
    "type": "object",
    "required": [
      "app_name",
      "user_id"
    ],
    "properties": {
      "user_id": {
        "type": "string",
        "description": "The ID of the user whose data is being synchronized."
      },
      "app_name": {
        "type": "string",
        "description": "The name of the third-party app from which to sync data."
      }
    }
  }
}