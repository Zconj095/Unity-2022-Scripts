{
  "name": "determine_season_from_date",
  "description": "Determines the season based on the month of the provided date.",
  "strict": false,
  "parameters": {
    "type": "object",
    "required": [
      "date"
    ],
    "properties": {
      "date": {
        "type": "string",
        "format": "date",
        "description": "The date for which to determine the season, formatted as 'YYYY-MM-DD'."
      }
    }
  }
}